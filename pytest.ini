[pytest]
# The tests import the app as `src.*`, so the repo root has to be importable.
# Without this a bare `pytest` — the command in the README, and the one CI
# runs — fails collection with "No module named 'src'"; only `python -m
# pytest` worked, because -m puts the working directory on sys.path.
pythonpath = .
testpaths = tests
